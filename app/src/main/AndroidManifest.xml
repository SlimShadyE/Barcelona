<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.aseel.myfinalapp">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".LogoActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".TopScorers" />
        <activity android:name=".ShareActivity" />
        <activity android:name=".SignUpR" />
        <activity android:name=".SignInAactivity" />
        <activity android:name=".Team" />
        <activity android:name=".Menu" />
        <activity android:name=".Games" />
        <activity android:name=".Club" />

        <receiver
            android:name=".MyReceiver"
            android:enabled="true"
            android:exported="true" />

        <service
            android:name=".MyIntentService"
            android:exported="false" />
        <service
            android:name=".MusicService"
            android:enabled="true"
            android:exported="true" />

        <activity android:name=".TrophiesActivity" />
        <activity android:name=".CameraActivity" />
        <activity android:name=".UclActivity"></activity>
    </application>

</manifest>